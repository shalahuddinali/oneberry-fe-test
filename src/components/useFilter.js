import React from 'react';

const useFilter = (data, agency, rangeSelected, year, contractAmountRange) => {
	const filterAgency = () => {};

	const filteredData = data.fiter(filterAgency());

	return <div>useFilter</div>;
};

export default useFilter;
